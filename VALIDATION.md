# 验证记录

验证日期：2026-09-25。已将 0.1.1 的原生 JavaScript 界面迁移为 React + TypeScript（strict）+ Vite + shadcn/ui + ESLint + Vitest + Zustand。

## 重构与行为保留

- 应用代码全部迁为 `.ts` / `.tsx`，启用 `strict`、未使用变量及参数检查；没有用 `any`、`@ts-ignore` 或 `@ts-nocheck` 绕过应用类型检查。
- React 组件拆分为连接面板、键盘布局、按键编辑器和对话框。shadcn/ui 源码组件使用 Radix 的焦点管理与键盘交互；主题保持现有深色键盘界面。
- Zustand 管理表单、设备视图、操作状态、备份列表、日志和确认弹窗。协议、编辑模型、IndexedDB 与 WebMCP 模块独立于 React。
- `HIDSession` 在入口创建并管理，启动幂等，不在组件 effect 中建立连接。首次请求授权仍直接发生在按钮点击调用链中。
- 保留每个连接世代只自动读取一次、重连时保留编辑内容、固件一致性、三组 / 九组配置、Fn 同步、扩展记录逐字节保留、写前备份事务及写后回读。
- JSON、Windows `.pro`、备份数据库名称和 schema 均保留；页面工具只暂存编辑，不能授权 USB 或执行硬件写入。
- Vite 生产构建继续输出单文件 HTML。CSP 使用实际内联脚本 SHA-256，保留 `connect-src 'none'` 和 HID 响应头。脚本在 `#root` 节点之后执行；React 内部的 HTML 字符串不参与外部资源标签替换。

## 自动验证

- 迁移前运行 Node 内置测试：83 项通过，作为行为基线。
- 原协议、HID、转换、备份事务与页面工具测试迁入 Vitest；保留原有故障注入和黄金报文断言。这些故意传入非法数据的历史测试保留 JavaScript，新状态与组件测试采用 TypeScript。
- 新增 Zustand 测试：自动读取、首次授权、重连保留输入、离线导入、错误恢复、取消确认、确认写入、确认期间断线、备份失败及页面工具状态同步。
- React Testing Library + jsdom 验证 66 键渲染、编辑 / 还原 / Fn 切层、方向键导航、按键计数、StrictMode、确认弹窗焦点及帮助弹窗。
- 产物测试通过 Vite 构建完整页面，校验内联脚本、CSP 哈希、无外部运行时资源、发布目录边界和 HID 响应头，并在 jsdom 中执行生成的 HTML，确认 66 键成功挂载且没有脚本启动错误。

本轮最终结果：

- 使用锁文件执行 `npm ci --offline` 成功（333 个包），确认可重复安装。
- `npm run typecheck`：通过。
- `npm run lint`：通过，0 错误、0 警告。
- `npm test`：8 个测试文件、93 项测试全部通过，0 失败。
- `npm run build`：通过，生成 `dist/index.html` 与 `dist/_headers`。
- `npm run check`：完整通过。

## 简体中文 / 英文国际化

- 新增类型安全的双语资源与语言切换控件。优先使用本地保存的语言，其次匹配浏览器语言；未知语言回退为简体中文。存储拒绝访问时可继续在当前页面切换。
- 页面标题、文档语言、描述、界面标签、设备状态、错误、确认弹窗、备份标签、日期、操作记录及可选页面工具随语言更新。
- Zustand 保存消息标识与参数；已有日志及弹窗可重新渲染，无需重新连接设备。切换保留未保存文本、选键、编辑层、颜色输入及待写入配置。
- 256 个中英文按键名称均能往返同一报文代码，支持混合语言输入；宏延迟、三层 Fn 同步、九组扩展数据与 JSON / `.pro` 格式保持兼容。已有备份的原因字段仅在显示时翻译，不修改存储内容。
- 全部翻译内联到生产单 HTML，没有新增运行时依赖或翻译网络请求。
- 新增 18 项测试，涵盖资源占位符、语言匹配 / 持久化 / 存储异常、键码及宏往返、草稿保留、确认流程、工具注册、真实 React 语言选择控件、英文错误与历史备份显示。
- 产物测试在 jsdom 中设置中文浏览器与已保存的英文偏好，执行完整生成 HTML，验证英文界面、标题、文档语言及页面描述；单文件与 CSP 检查同时通过。
- 本轮 `npm run check` 通过：TypeScript strict、ESLint、11 个测试文件中的 **111 项测试**及 Vite 生产构建均成功。上文 93 项为框架迁移时的基线记录。

## 仍未完成的验证

- 没有通过网页对真实键盘执行首次授权、读取、写入或 RGB 操作。本次没有修改真实键盘配置。
- jsdom 属于自动化 DOM 测试，不是浏览器视觉验收，不能验证 WebHID 权限对话框、真实 USB 传输、真实 IndexedDB 持久化及浏览器 CSP 执行。
- Windows XML 转换保留原有替身测试；真实浏览器 DOMParser 与 `.pro` 导入仍需验收。
- 没有支持 WebMCP 的宿主，注册和编辑流程仅经过模拟测试。
- `file://` 的 USB 和 IndexedDB 权限未作实机验证，正式使用推荐独立 HTTPS 页面。
- 之前的私人 66EC-S / V1.4.4 样本回放记录仍有效：594 报文、9 组完整往返；本次未访问或重新回放该私人样本。`npm run replay -- <项目外路径>` 可由持有样本者复核。

## 当前环境与发布

- npm registry DNS 不可用；本轮从本机已有 npm 缓存安装依赖，使用原始包归档及其完整性信息，锁文件保留标准 registry URL，未修改包代码。
- 本地 Vite 预览监听 `127.0.0.1:5173` 返回 `EPERM`，未更换端口或调整安全限制，预览未启动。
- 已核对原 Sites 项目：仍为仅本人可见，版本数 0，无在线地址。Git 仓库已由用户初始化，但本轮 `git add --all` 因无法创建 `.git/index.lock` 返回 `Operation not permitted`，仍无法保存提交并按 Sites 流程推送及发布。项目标识与静态发布目录保持不变，没有重建站点或扩大访问范围。

## 首次实机验收顺序

1. 使用独立的、允许 WebHID 的浏览器来源；关闭原生配置工具并以 USB 连接键盘。
2. 用户在页面手动完成首次授权。应显示“已连接”，随后自动读取配置并尝试保存备份，不自动写入配置。
3. 无需点击读取，核对自动载入的 594 条记录与三层内容，下载 JSON 备份；拔插后应自动重连并读取。如果有未写入修改，应保留旧编辑区且禁止直接写入，待用户重新载入或导入后再操作。
4. 只有用户明确选择写入时，修改一个不影响 Fn 的按键，查看确认摘要，验证写前备份、回读成功以及键盘实际行为。
5. 重新读取、导入先前备份并由用户确认恢复；RGB 操作另用 RGB 型号验收。

若授权后仍无法连接，请保留页面操作记录或错误文字。若读取失败，下载“读取诊断”；不要把诊断文件当作恢复备份写入。
